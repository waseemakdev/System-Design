What is Networking and why we need?

is the framework for how independent computing components (servers, databases, clients) communicate over a network, encompassing protocols, hardware (routers, switches), and topologies.

What is TCP and UDP?

Caches and Routes?

Why networks are working?

Client Server Architecture.

Vertical Scalling

Horizontal Scalling

API Calling

Client Request and Response 

How domain store their name by using IP like github.com when we call this github they have a IP address that is calling to this specific domain
to server.

DNS (Domain Name Server)

Multiple Domains
TLD( Top Level Domain)
Authoritative Domain mean is we take server as an example. They store multiple domain address at on same time. for example they store multiple domains
IP at a time.
We call this a root than it call TLD and than it call Authoritative domain and than furhter it call authoritative domains to call the specific address they want.


Load Balancing OR Load Balancer

IP Address

Mac Address

Port Number

Algorithum's

Round Robin




